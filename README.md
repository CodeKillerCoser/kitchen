# 岐黄创意厨坊 (QiHuang Creative Kitchen)

一款融合了中医食养智慧与现代AI技术的智能膳食规划应用。它旨在帮助追求效率的职场人士轻松制定健康、美味且个性化的周度饮食计划。

## ✨ 主要功能

- **智能周度排餐**: 根据用户选择的食养方向（如“健脾祛湿”、“养颜滋阴”等）和个人偏好，一键生成为期7天的完整膳食计划。
- **AI大厨问答**: 对食谱或烹饪有任何疑问？随时可以向内置的AI大厨提问，获得专业的指导。
- **动态菜谱详情**: 查看每道菜的详细信息，包括所需食材、详细步骤、食养益处、烹饪禁忌等。
- **AI生成菜谱图**: 通过AI为菜品的关键步骤生成参考图片，让烹饪更直观。
- **AI语音朗读**: 可以让AI大厨用语音为您朗读菜谱步骤或问答结果。
- **可配置API**: 用户可以在设置中自行配置兼容OpenAI格式的API Key和Base URL，并选择使用的模型。

## 🛠️ 技术栈

- **前端**: React, Vite, TypeScript, Tailwind CSS
- **AI集成**: 通过 `fetch` 调用兼容 OpenAI 格式的 API
- **打包构建**: 使用 [Tauri](https://tauri.app/) 将Web应用打包成跨平台桌面及移动应用。
- **CI/CD**: 通过 [GitHub Actions](.github/workflows/build-android.yml) 自动化构建安卓应用。

## ⚙️ 配置

为了让应用能够正常调用AI模型，您需要进行以下配置：

1.  点击右下角的 **设置** 图标。
2.  **API Key**: 填入您的AI服务提供商的API密钥。
3.  **Base URL (可选)**: 填入API的基地址 (例如, `https://api.openai.com/v1`)。如果留空，将默认使用官方OpenAI地址。
4.  **选用模型**: 点击 **加载模型** 按钮，应用会自动获取并填充可用的模型列表。从下拉菜单中选择您想使用的模型。
5.  点击 **保存设置**。

## 🚀 本地开发

1.  **克隆仓库**:
    ```bash
    git clone https://github.com/CodeKillerCoser/kitchen.git
    cd kitchen
    ```

2.  **安装依赖**:
    ```bash
    npm install
    ```

3.  **运行开发服务器**:
    应用将在本地启动一个Web服务。
    ```bash
    npm run dev
    ```

4.  **运行Tauri桌面版 (可选)**:
    如果您本地安装了完整的Tauri开发环境（包括Rust），可以使用以下命令启动桌面版应用进行调试。
    ```bash
    npm run tauri dev
    ```

## 🤖 安卓应用构建

本项目配置了GitHub Actions，可在云端自动构建Android应用（APK）。

- **触发条件**: 每次向 `main` 分支推送代码时，工作流会自动触发。
- **构建产物**: 构建成功后，您可以从对应工作流的 "Artifacts"（产物）区域下载 `.apk` 安装包。

### 签名配置 (重要)

为了成功构建可安装的APK，您**必须**在您的GitHub仓库中设置用于签名的密钥。

请参考 [Tauri官方安卓签名文档](https://tauri.app/v1/guides/distribution/android/#signing) 生成签名文件，并将其内容设置为仓库的Secrets。需要设置以下两个Secrets：

- `TAURI_SIGNING_PRIVATE_KEY`
- `TAURI_SIGNING_PRIVATE_KEY_PASSWORD`

具体路径：**GitHub仓库** -> **Settings** -> **Secrets and variables** -> **Actions** -> **New repository secret**。
